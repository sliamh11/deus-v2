# Agent-Agnostic Technical Debt

This file tracks the remaining open-ended work between today's
backend-neutral foundation and a fully agent-agnostic Deus.

Rules:

- Every row needs a stable debt ID.
- Every row must name the affected surface and user-visible risk.
- Every row must have explicit exit criteria.
- If a change intentionally leaves parity or onboarding incomplete, update this
  file in the same diff.

## Active Debt

| ID | Surface | Why still open | User-visible risk | Exit criteria |
|---|---|---|---|---|
| `AAG-010` | Codex hook parity | Codex hooks mirror every current Claude Code hook with public-safe equivalents, but some parity remains best-effort: Codex plan-mode invalidation is inferred from exposed events and `/plan` prompts, Stop checkpointing still needs live Codex transcript-format verification, and Claude's private local orchestrator preflight is default-off behind `DEUS_CODEX_ORCHESTRATOR_PREFLIGHT=1`. Hook interception is still a guardrail rather than a complete boundary for every shell or non-MCP path. | A Codex session can still need manual marker invalidation after unusual plan flows, Stop checkpoints may miss context until live-verified, local orchestrator health is not checked unless explicitly enabled, and unusual edit/merge paths may bypass local guardrails until review/CI catches them. | Codex exposes hook events/tool coverage equivalent to Claude Code for plan-mode/subagent transitions and all write-capable tool paths, Stop hook transcript compatibility is verified by fixture or live test, public-safe orchestrator preflight is configured by setup, and `scripts/codex_warden_hooks.py check` verifies the full hook set end-to-end. |
| `AAG-012` | `/deep-research` skill | Uses Claude Code-specific tools: Agent tool for subagent fan-out (lit-scout, brainstormer), NotebookLM MCP (host-only), AskUserQuestion for scope clarification. Container runtime and Codex backend lack these interfaces. **Partially mitigated (LIA-444, 2026-07-18):** `deus-native`'s `dispatch_nested_agent` tool now resolves the checked-in `.claude/agents/researcher.md` role (real methodology + `{content}` output contract) when the parent model targets it by name — chat-reachable on any channel the `deus-native` backend serves. This is a single-role, model-mediated dispatch (no deterministic trigger, no multi-agent fan-out, no NotebookLM), not the full `/deep-research` pipeline. | Skill is invisible on Codex backend and unreachable from chat channels (WhatsApp, Telegram, Slack, Discord, Gmail, Teams, Outlook) via the actual `/deep-research` skill. **Narrowed for `deus-native` users specifically:** a single-pass Researcher delegation is now reachable by asking the model to delegate to the researcher role, but this is not equivalent to `/deep-research`'s full lit-scout/brainstormer fan-out + NotebookLM synthesis. | Container-side variant using TeamCreate/SendMessage for fan-out, or a degraded single-pass mode using only WebSearch + vault memory. Alternatively, a container `agent.ts` that proxies the pipeline via IPC. |
| `AAG-015` | TUI channel-status parity (Teams/Outlook) | **Partially mooted by LIA-389 (2026-07-17):** the Rust TUI (`tui/`, including `tui/src/config/channels.rs`) was archived and removed from main — see `docs/decisions/tui-archival.md` and `legacy/tui-phase1`. The remaining reference, `packages/tui/src/channel-status.ts:30-47` (`getChannelStatuses()`), is a separate TypeScript/Ink TUI prototype that appears abandoned independent of this decision (last commit 2026-05-05, not in root `package.json` workspaces, not wired into `deus-cmd.sh`/`deus-cmd.ps1`, not mentioned in README.md) — it was not evaluated or touched by LIA-389, which was scoped to the Rust TUI only. Still hardcodes the same pre-Teams/Outlook 6-entry channel array. | With the Rust TUI gone, only `packages/tui/`'s status view (if it's ever run) fails to show Teams/Outlook. Whether that package is live enough for this to be a real user-visible risk, or whether it should itself be archived/removed, needs a separate triage decision — out of scope for LIA-389. | If `packages/tui/` is kept: add `{ name: 'Microsoft Teams', configured: ... }` / `{ name: 'Outlook', configured: ... }` entries to `channel-status.ts`, using the correct config-detection signal (`TEAMS_APP_ID`+`TEAMS_APP_PASSWORD` per `packages/mcp-teams/README.md`; `OUTLOOK_CREDENTIALS_DIR`/`app-credentials.json` per `packages/mcp-outlook/README.md`), landed alongside a status-panel test. If it's dead code, close this row as moot upon its removal instead. |
| `AAG-013` | `deus-cmd.ps1` `--print-identity` parity | Flag shipped bash-only (GH #1004): the consuming VS Code `claudeProcessWrapper` shim is macOS-first, and the ps1 port needs its own print+exit interception before each `Invoke-Agent @("--append-system-prompt", ...)` call site (deus-cmd.ps1:354/466/468) plus stdout/stderr stream separation. | On Windows, `deus --print-identity` falls through to usage text; VS Code extension-managed sessions cannot splice the Deus identity prompt. | `deus-cmd.ps1` prints the identical payload at all `--append-system-prompt` call sites with progress noise kept off stdout, or the planned TS port of deus-cmd supersedes the ps1 script. |
| `AAG-014` | `deus-native` memory middleware for non-control groups | LIA-415/D1 wired `scripts/memory_retrieval_hook.py` into the `deus-native` `beforeModel` memory layer for control-group turns only. The hook retrieves from the user's personal vault and has no group scoping, so invoking it for arbitrary groups would leak personal context across unrelated conversations — the same isolation constraint the pre-D1 placeholder documented. | Non-control groups on `deus-native` get no per-prompt memory recall: a visible parity gap versus control-group turns (and versus the container path's own memory surfaces), so those groups appear to "remember" less. | A group-scoped retriever serves non-control groups (per-group memory store or a scoping filter inside `recall()`), landed with isolation tests proving no cross-group or personal-vault leakage and injection-framing tests preserving the hook's untrusted-memory boundary. |
| `AAG-016` | Container-side `deus-native` session and orchestration parity | LIA-423 proves direct, non-default container protocol portability, but keeps conversation messages only in the runner process and deliberately does not import/rebuild the host-native middleware, hooks, checkpointer, lifecycle/context, compaction, nested-dispatch, or usage stack. There is no production registry caller for this path. | A direct experimental caller can keep multi-turn continuity only for one live container process; restarting the container with its returned `RuntimeSession` preserves identity/metadata but not prior message state. Treating the path as production-equivalent would silently lose host-native policy and continuity features. | Before any production routing change: write a superseding ADR that assigns middleware/checkpoint/context ownership; add durable backend-scoped message persistence and cross-container resume tests; prove permission, warden, memory, compaction, nested-dispatch, usage, and public-ingress behavior; then deliberately add and test an explicit production opt-in without weakening the host-native default. |
| `AAG-017` | `deus-native` Linear pipeline coding is capability-guarded, not implemented | LIA-422/E3 wires `deus-native` as a selectable Linear pipeline backend and adds an explicit capability-readiness guard (`assessDeusNativeCapabilityReadiness`/`assessDeusNativePipelineReadiness` in `src/agent-runtimes/deus-native-pipeline-readiness.ts`), but the host-side `deus-native` tool surface (`DEUS_NATIVE_SAFE_TOOL_NAMES` in `tool-broker-langchain-adapter.ts`) still exposes only `web_search`/`web_fetch` — no `apply_patch`, no commit-capable `Bash`. See `docs/decisions/linear-pipeline-deus-native-capability-boundary.md`. | Selecting `deus-native` as the Linear pipeline's backend today refuses every issue (parked in Manual Review Required, labeled `runtime:capability-blocked`) rather than silently attempting — by design — but the pipeline cannot actually complete coding work through `deus-native` until this lands. | A separately-filed, separately-reviewed ticket implements the B5/`deus-v2-replay-safety.md` claim/completion idempotency contract and a worktree-confined, isolated coding tool surface (read/patch/verify/commit) for `deus-native`, at which point `probeDeusNativePipelineReadiness()` returns ready and the guard stops firing for real pipeline issues. |

## Recently Closed

| ID | Closed by | Resolution |
|---|---|---|
| `AAG-C001` | `feat/agent-backend-abstraction` worktree | `AGENTS.md` became the canonical onboarding source, `CLAUDE.md` was reduced to a compatibility mirror, and runtime-loaded `AGENT_DEUS_101.md` onboarding was removed from the always-load context path. |
| `AAG-C002` | `feat/agent-backend-abstraction` worktree | `deus backend [show\|set\|model\|list]` CLI command added to both `deus-cmd.sh` and `deus-cmd.ps1`. Persists to both `~/.config/deus/config.json` and `.env`. |
| `AAG-C002a` | PR #252 (`feat/add-codex-skill`) | `/add-codex` interactive skill shipped: guided API key setup, backend config, parity warnings, verification, and troubleshooting. Resolves AAG-007. |
| `AAG-C003` | `feat/close-agnostic-debt` | Responses API chosen over Agents SDK. Decision documented in the backend-neutral ADR. Handoffs/tracing deferred as optional accelerators. Resolves AAG-002. |
| `AAG-C004` | `feat/close-agnostic-debt` | Supported backend boundary documented in `MULTI_BACKEND.md`: Claude and OpenAI/Codex are the implemented adapters; `ollama` is a forward reservation. Resolves AAG-003. |
| `AAG-C005` | `feat/close-agnostic-debt` | `CLAUDE.md` frozen as permanent compatibility shim — required by the Claude Code CLI convention (`CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD`). The file is a thin redirect to `AGENTS.md`. Resolves AAG-004. |
| `AAG-C006` | `feat/close-agnostic-debt` | Skill parity audit complete. All skills except `x-integration` (broken on both backends — wrong export, unfinished wiring) and `add-ollama-tool` (OpenAI backend hardcodes MCP server list) are instruction-only and work on both backends. Known gaps documented. Resolves AAG-005. |
| `AAG-C007` | Qodo removal (2026-04-25) | Qodo was removed from the project entirely. Discovery parity is no longer applicable. Resolves AAG-006. |
| `AAG-C008` | `feat/close-agnostic-debt` | Orchestrator and scheduler now call `backend.runTurn()` instead of `runContainerAgent()` directly. `turn_complete` event added to `RuntimeEvent`. `onProcess` removed from `SchedulerDependencies`. Resolves AAG-009. |
| `AAG-C009` | PR #258 + live E2E (2026-04-26) | Codex OAuth landed and verified E2E on host: startup gate passes, auth provider reads `~/.codex/auth.json` JWT, `injectAuth()` injects real token, auth-refresh daemon detects fresh token. Resolves AAG-001. |
| `AAG-C010` | `feat/portable-warden-gates` | All gate/invalidation/verdict-tracking logic consolidated in `scripts/codex_warden_hooks.py`. Claude Code on macOS/Linux uses `.claude/hooks/warden-shim.sh` (project-level, auto-loaded on clone). Codex and Windows Claude Code use `install` subcommand. Added: verdict tracking, audit log, post-REVISE bypass prevention, `mark` subcommand. Resolves AAG-011. |
