# code-reviewer scope memo (LIA-512 re-review, post ai-eng-warden touch-ups)

Repo: sliamh11/deus-v2 worktree lia512-memory-server-env. Diff: 3 files, +51/-14
(.github/workflows/ci.yml, deus_memory_client/servers.py, deus_memory_client/tests/test_servers.py).
Re-review of a diff already SHIP'd by code-reviewer (claude+gpt) and ai-eng-warden, plus 2
informational touch-ups the author applied after ai-eng-warden's flags (mcp CI pin, stale
env-merge comment fix).

Verified independently (not trusted from author's summary):
- Read mcp==1.27.1's actual `mcp/client/stdio/__init__.py` (installed in .venv, version
  confirmed via `pip show mcp`). Line 127: `env=({**get_default_environment(), **server.env}
  if server.env is not None else get_default_environment())` — confirms the corrected comment's
  claim (union-over-default, not replace) and the new SHELL/TERM note (always in default,
  survives regardless of ENV_ALLOWLIST contents).
- `evolution/requirements.txt:11` has `mcp==1.27.1` — CI pin matches exactly.
- Ran `pytest deus_memory_client/tests/ -v` directly: 24/24 passed.
- `yaml.safe_load`'d ci.yml: valid.
- Full `git diff` matches description exactly — no undescribed changes.

No blocking/warning findings. Verdict: SHIP.
