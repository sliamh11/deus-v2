// Inert stub — see package.json description. Only satisfies the three named
// imports @assistant-ui/core's dist/react barrel statically pulls in
// (AssistantCloud, CloudMessagePersistence, createFormattedPersistence).
// None of these are exercised by this prototype's LocalRuntime-only usage.
export class AssistantCloud {}
export class CloudMessagePersistence {}
export function createFormattedPersistence() {
  return {};
}
